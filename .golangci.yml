run:
  deadline: 5m
  tests: true

linters:
  enable:
    - gofmt
    - govet
    - staticcheck
    - errcheck
    - gosimple
    - ineffassign
    - unused
    - misspell
    - bodyclose
    - noctx

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - noctx
