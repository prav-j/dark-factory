specs/

# Environment & secrets — never commit
.env
.env.*
*.pem
*.key

# Build artifacts & coverage
/bin/
coverage.out

# Built binaries (root only)
/mockoidc
/registry
/orchestrator

# Conformance reports are CI artifacts
conformance/report.json

# scenario artifacts
create.json
pub.json
